# Core dependencies — install all for full framework
# Or install selectively per chapter (see pyproject.toml optional dependencies)

# LLM Providers (install only what you use)
openai>=1.30.0
anthropic>=0.25.0
google-generativeai>=0.7.0

# Async
anyio>=4.0.0
httpx>=0.27.0

# Schema validation
pydantic>=2.7.0

# Vector RAG (Chapter 6)
sentence-transformers>=3.0.0
numpy>=1.26.0

# Telemetry (Chapter 9)
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0

# Optional: Graph RAG (Chapter 6, graph path)
# neo4j>=5.20.0

# Optional: Production session storage (Chapter 5)
# redis>=5.0.0

# Dev
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
